<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.panda.merge.dao.I18nOutrightMarketDao">


    <insert id="saveBatch">
        insert into i18n_outright_market (
            name_code,data_source_code, language_type, text, remark,create_time, modify_time
        ) values
        <foreach collection="i18nOutrightMarketList" item="item" index="index" separator=",">
        (
            #{item.nameCode}, #{item.dataSourceCode}, #{item.languageType}, #{item.text}, #{item.remark},
            REPLACE(unix_timestamp(current_timestamp(3)),'.',''),REPLACE(unix_timestamp(current_timestamp(3)),'.','')
        )
        </foreach>
    </insert>

    <update id="updateBatchById">
        <foreach collection="i18nOutrightMarketList" item="item" index="index" open="" close="" separator=";">
            UPDATE i18n_outright_market set
            <if test="item.text != null">
                text = #{item.text},
            </if>
            <if test="item.remark != null">
                remark = #{item.remark},
            </if>
            modify_time = REPLACE(unix_timestamp(current_timestamp(3)),'.','')
            WHERE id = #{item.id,jdbcType=BIGINT}
        </foreach>
    </update>

</mapper>