<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.panda.merge.dao.LanguageInternationDao">

    <insert id="insertList" parameterType="java.util.List" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO language_internation(id, name_code, data_source_code, language_type, text, remark, create_time, modify_time) VALUES
        <foreach collection="list" item="item" index="index" separator=",">
            (
            #{item.id}, #{item.nameCode}, #{item.dataSourceCode}, #{item.languageType}, #{item.text}, #{item.remark},
            REPLACE(unix_timestamp(current_timestamp(3)),'.',''),
            REPLACE(unix_timestamp(current_timestamp(3)),'.','')
            )
        </foreach>
    </insert>

    <update id="updateList" parameterType="java.util.List">
        <foreach collection="list" item="item" separator=";">
            UPDATE language_internation
            <set>
                text = #{item.text},
                <if test="item.remark != null">
                    remark = #{item.remark},
                </if>
                modify_time = REPLACE(unix_timestamp(current_timestamp(3)),'.','')
            </set>
            WHERE id = #{item.id}
        </foreach>
    </update>
</mapper>