<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.panda.merge.dao.I18nOutrightMarketOddsDao">


    <insert id="saveBatch">
        insert into i18n_outright_market_odds (
        name_code, flag, data_source_code, language_type, text, remark, create_time, modify_time
        ) values
        <foreach collection="i18nOutrightMarketOddsList" item="item" index="index" separator=",">
            (
            #{item.nameCode}, #{item.flag}, #{item.dataSourceCode}, #{item.languageType}, #{item.text}, #{item.remark},
            REPLACE(unix_timestamp(current_timestamp(3)),'.',''),REPLACE(unix_timestamp(current_timestamp(3)),'.','')
            )
        </foreach>
    </insert>


</mapper>