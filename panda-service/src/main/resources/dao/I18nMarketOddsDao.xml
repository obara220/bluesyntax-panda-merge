<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.panda.merge.dao.I18nMarketOddsDao">


    <insert id="saveBatch">
        insert into i18n_market_odds (
        odds_source_id, data_source_code, language_type, text, create_time, modify_time
        ) values
        <foreach collection="i18nMarketOddsList" item="item" index="index" separator=",">
        (
            #{item.oddsSourceId},  #{item.dataSourceCode}, #{item.languageType}, #{item.text},
            REPLACE(unix_timestamp(current_timestamp(3)),'.',''), REPLACE(unix_timestamp(current_timestamp(3)),'.','')
        )
        </foreach>
    </insert>

    <update id="updateBatchById">
        <foreach collection="i18nMarketOddsList" item="item" index="index" open="" close="" separator=";">
            UPDATE i18n_market_odds set
            <if test="item.text != null">
                text = #{item.text},
            </if>
            modify_time = REPLACE(unix_timestamp(current_timestamp(3)),'.','')
            WHERE odds_source_id = #{item.odds_source_id,jdbcType=BIGINT}
        </foreach>
    </update>
</mapper>